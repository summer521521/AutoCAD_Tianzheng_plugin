name: Plugin Check

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  check:
    runs-on: windows-latest
    defaults:
      run:
        shell: pwsh
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Check plugin structure
        run: .\scripts\check-plugin.ps1
      - name: Privacy scan
        run: .\scripts\check-repo-privacy.ps1
